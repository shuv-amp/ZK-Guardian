{
    "name": "circuits",
    "version": "0.0.1",
    "private": true,
    "description": "ZK Guardian Circom circuits",
    "license": "Apache-2.0",
    "scripts": {
        "compile": "bash scripts/compile.sh",
        "trusted-setup": "bash scripts/trusted-setup.sh",
        "test": "mocha test/**/*.test.js --timeout 120000 --exit",
        "clean": "rm -rf build/*.wasm build/*.r1cs build/*.zkey build/*.json"
    },
    "devDependencies": {
        "chai": "^4.3.10",
        "circomlibjs": "^0.1.7",
        "mocha": "^10.2.0",
        "snarkjs": "^0.7.3"
    },
    "dependencies": {
        "circomlib": "^2.0.5"
    }
}