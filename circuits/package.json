{
    "name": "circuits",
    "version": "0.0.1",
    "private": true,
    "description": "ZK Guardian Circom circuits",
    "license": "Apache-2.0",
    "scripts": {
        "pretest": "bash scripts/ensure-artifacts.sh",
        "compile": "bash scripts/compile-all.sh",
        "setup": "bash scripts/setup-all.sh",
        "trusted-setup": "bash scripts/setup-all.sh",
        "test": "mocha test/AccessIsAllowedSecure.test.js --timeout 120000 --exit",
        "test:legacy": "mocha test/accessIsAllowed.test.js --timeout 120000 --exit",
        "clean": "rm -rf build/*"
    },
    "devDependencies": {
        "chai": "^4.3.10",
        "circomlibjs": "^0.1.7",
        "mocha": "^10.2.0",
        "snarkjs": "^0.7.3"
    },
    "dependencies": {
        "circomlib": "^2.0.5"
    }
}
